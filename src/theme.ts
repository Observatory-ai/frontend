import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#24212b',
    },
    secondary: {
      main: '#ff5851',
    },
    background: {
      default: '#f4f5fe',
    },
  },
});

export default theme;
